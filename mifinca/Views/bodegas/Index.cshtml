@model IEnumerable<mifinca.Models.bodega>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.finca.foto_finca)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.finca.foto_finca)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_bodega }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_bodega }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_bodega })
        </td>
    </tr>
}

</table>
